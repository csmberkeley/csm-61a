\begin{blocksection}
\question Assign the name, \lstinline$t$, to the tree to the right.

\begin{solution}[1.5in]
\begin{lstlisting}
t = tree(9, [tree(2),
             tree(4, [tree(1)]),
             tree(4, [tree(7),
                      tree(3)])])
\end{lstlisting}
\end{solution}
\end{blocksection}
